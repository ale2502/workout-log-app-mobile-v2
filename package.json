{
  "name": "workout-log-app-mobile",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "concurrently": "^9.2.1"
  },
  "scripts": {
    "update:mobile-env": "node scripts/update-mobile-env.mjs",
    "dev:api": "npm run dev -w apps/api",
    "dev:mobile": "npm run update:mobile-env && npm start -w apps/mobile"
  }
}
