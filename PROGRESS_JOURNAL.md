# Progress Journal

Run the Reflect workflow after each study block and record the outcomes here. Log tutoring sessions, architecture decisions, setup milestones, and follow-up tasks for this workout log app.

| Date | Activity or Question | What I Asked / Did | What I Learned | Next Action |
|------|----------------------|--------------------|----------------|-------------|
| 2026-04-05 | Tutor workflow: define the new project shape | Decided to start from scratch in a new repo and discussed how to structure it for mobile first, backend included, and web later. | A clean starting point is one repo with separate responsibilities: `apps/mobile`, `apps/api`, and `packages/shared`. Shared code should contain reusable types, validation, and pure logic rather than UI or server-specific code. | Decide the first setup milestone and explain why that setup order makes sense. |
| 2026-04-05 | Tutor workflow: workspace setup and Expo scaffolding | Created the monorepo folder structure, added a root workspace `package.json`, created child package files, cleaned up PromptKit and `GEMINI.md`, and scaffolded an Expo app into `apps/mobile`. | The root package organizes the monorepo, while child packages own their own dependencies. Expo generated a working mobile app structure in `apps/mobile`, including its own app `package.json` and nested `.gitignore`. | Next session, verify the Expo app runs, review the generated files, and decide how to set up `apps/api`. |
| 2026-04-06 | Tutor workflow: verify Expo and define API structure | Ran the Expo app successfully, opened it on an Android phone with Expo Go, compared preview options (`web`, Android emulator, iOS simulator, Expo Go), and decided to move quickly with Android only for now. Then chose a backend architecture where `apps/api` owns a TypeScript server, SQLite, Knex, migrations, and seeds in a bootcamp-style `server/` folder. Created the initial `apps/api/server/` folders and placeholder files. | Expo is already working; Android and iOS errors were local tooling issues, not app failures. Expo Go on a real Android phone is enough to continue building. The backend should follow the familiar bootcamp pattern inside `apps/api`, with routes, DB connection, Knex config, migrations, and seeds kept together under `server/`. | Finish `apps/api/package.json`, install backend dependencies, and get the first API startup path working before adding migration and seed logic. |
| 2026-04-12 | Tutor workflow: first API boot path | Reviewed where the project left off, confirmed how to start the Expo mobile app again, completed `apps/api/package.json` and `tsconfig.json`, added `server/index.ts`, `server.ts`, and `routes/index.ts`, fixed the router export issue, and ran `npm install` plus `npm run dev` successfully. | The first backend milestone is just proving the API boots. `index.ts` starts the server, `server.ts` configures Express, `routes/index.ts` is the main router, and feature files like `routes/exercises.ts` handle one route area each. The API now runs on `http://localhost:3001` and responds on `/health`. | Next session, add the first `exercises` route and then set up SQLite and Knex without mixing DB setup into the initial server boot step. |
| 2026-04-18 | Tutor workflow: review route flow and plan the DB step | Reviewed where the backend work stopped, walked through the route flow again, confirmed how `index.ts`, `server.ts`, and `routes/exercises.ts` fit together, and saved a concise summary of the next database milestone for tomorrow. | The route layer is already in place, and the DB files exist as placeholders. The next clean move is to keep the route thin, introduce Knex and SQLite in the DB layer, define schema through migrations, and then replace the hard-coded `/exercises` response with a real database query. | Start with Knex and SQLite setup, create the first `exercises` table, and then update `GET /exercises` to read from the database. |
| 2026-04-19 | Tutor workflow: Knex, SQLite, ESM, and first exercise table | Installed `knex` and `sqlite3`, switched the API package toward bootcamp-style ESM with `"type": "module"` and `tsx`, configured `server/db/knexfile.js`, added a Knex script, created and ran the `create_exercises` migration, created starter exercise seed data, and discussed where model/query functions belong. | CommonJS and ESM are module systems, while ES2020 is a language target. In this monorepo, backend database query functions should live under `apps/api/server/models`, while `server/db` should stay focused on connection/config/migrations/seeds. The better schema shape is `exercises`, `workouts`, and `sets`, with `sets` connecting workouts to exercises through `workout_id` and `exercise_id`. | Tomorrow, review `apps/api/server/models/exercise.ts` and the placeholder `apps/api/server/db/exercises.ts`, then create the first model query for reading exercises and update `GET /exercises` to use the database instead of hard-coded JSON. |
| 2026-04-20 | Tutor workflow: exercise DB query and workout/set schema planning | Reviewed the saved notes from 2026-04-19, clarified the bootcamp-style folder split, moved `/exercises` from hard-coded data to `db/exercises.ts`, mapped `muscle_group` to `muscleGroup`, confirmed the route returns `res.json(...)`, and planned the `workouts` and `sets` tables. Created draft migrations for `workouts` and `sets`. | `routes/exercises.ts` should only handle HTTP concerns, while `db/exercises.ts` should know the table and query details. `ExerciseData` is for new exercise input without an `id`, while `Exercise` is for saved rows with an `id`. `workouts.performed_on` means the day the workout happened and can default to today; `created_at` means when the row was inserted. `sets` is the table users will create most often and should store `exercise_id`, `workout_id`, set number, reps, load, reps in reserve, and note. Foreign keys protect relationships, joins read related data, and required fields prevent incomplete rows. | Tomorrow, continue with the `create_sets` migration. Review `.notNullable()` for required fields and foreign key syntax for `exercise_id` and `workout_id`, then decide whether to enforce those constraints now. |
| 2026-04-21 | Tutor workflow: required fields and ordered seeds | Reviewed the `sets` schema, decided which fields should be required, generated ordered seed files, fixed ESM export format, and debugged repeatable seed runs with foreign keys. | `sets` is the child table because it points to both `exercises` and `workouts`. Required fields should be the data that makes a set meaningful: exercise, workout, set number, and reps. Seed files need to delete child rows first, then parent rows, but insert parent rows before child rows. A plain `sets` model may be different from a joined/display model that includes exercise names or workout dates. | Next session, run the API server, then focus on the `sets` TypeScript model, `sets` database query functions, and how join query result shapes should be typed. |
| 2026-04-22 | Tutor workflow: set row and display model shapes | Asked whether `exercise_id` and `workout_id` are only for database joins, whether the frontend should instead show fields like exercise name, load, reps, RIR, note, and set number, and whether optional fields only need `?` in TypeScript. | A plain `Set` or `SetRow` model is useful for database operations because it keeps relationship IDs. A joined `SetDisplay` model is better for frontend-friendly API responses because it can include `exerciseName` instead of raw IDs. Optional database columns usually come back as `null`, so the model should often use `number | null` or `string | null` rather than only `?`. | Tomorrow, continue from `apps/api/server/models/sets.ts` and `apps/api/server/db/sets.ts`: choose clear names like `SetRow` and `SetDisplay`, represent nullable fields accurately, and finish the joined `getAllSets()` query. |
| 2026-04-23 | Tutor workflow: sets display query and new workout route | Fixed and tested `GET /sets`, updated `SetData` nullable fields, discussed whether to keep the bootcamp-style `SetData` / `Set` naming, planned the new workout flow, and confirmed `POST /workouts` works. | Joined display queries need specific column names after joins, such as `sets.id as id`, and selected columns must match the schema, such as `note` not `notes`. `SetData` can use `number \| null` and `string \| null` for fields the database allows to be empty. Creating a workout first gives the app a real `workoutId` that future sets can reference. | Add `POST /sets`: create an `addSet(newSet: SetData)` DB function, map camelCase API input to snake_case DB columns, insert into `sets`, return the created row, add `router.post('/')` in the sets route, and test it with the `workout.id` returned from `POST /workouts`. |
| 2026-04-25 | Tutor workflow: finish POST /sets and plan muscle group picker | Confirmed `POST /sets` was already implemented, tested it with a workout created by `POST /workouts`, changed successful POST responses to `201 Created`, added JSON `400` validation for missing `workoutId`, `exerciseId`, `setNumber`, or `reps`, and planned the next mobile flow. | The backend now supports the core workout logging loop: create a workout, add sets linked to that workout and an exercise, and read joined set display data. The next mobile screen should not jump straight to exercise selection; it should first fetch exercises, derive unique muscle groups, let the user pick a muscle group, and then filter exercises by that selected group. | Tomorrow morning, start in `apps/mobile/app/(tabs)/index.tsx`: fetch `GET /exercises`, store exercises in state, derive unique `muscleGroup` values, render muscle group buttons, and show filtered exercises after a muscle group is selected. |
| 2026-04-26 | Tutor workflow: mobile fetch flow | Walked through how to replace the Expo starter tab screen with a workout start screen, including `useState`, `useEffect`, fetching `GET /exercises`, using the computer's local IP address from Expo Go, deriving unique muscle groups with `map`, `Set`, and spread syntax, and filtering exercises after a muscle group is selected. | `HomeScreen` is just the exported component for `app/(tabs)/index.tsx`, not a built-in function. `useState` stores values that survive re-renders, `useEffect` runs the fetch after the first render, and derived variables such as `filteredExercises` are recalculated on every render. `selectedMuscleGroup` starts as `null`, then becomes truthy after a muscle group button calls `setSelectedMuscleGroup(...)`, causing React to re-render and show the matching exercises. | Tomorrow, continue from the current `index.tsx` work: finish or review the muscle group buttons, confirm filtered exercises display correctly, and then decide what tapping an exercise should do next. |
| 2026-04-27 | Tutor workflow: plan routed workout flow | Discussed that the current single-screen flow should become a clearer screen-by-screen workflow: Home with one `Start workout` button, then muscle group selection, then filtered exercise selection, then a set-entry screen for reps, load, RIR, and notes. | In Expo Router, each main app step can be its own route/screen file, while smaller repeated UI can become components later. The clean flow is: tap `Start workout`, call `POST /workouts`, receive a `workoutId`, pass that through the muscle group and exercise screens, then use it when creating sets. A `workoutId` must exist before `POST /sets` because `workoutId` is a mandatory relationship field; a set cannot be saved without belonging to a workout and an exercise. | Next session, start reshaping the mobile app around routes/screens: make the home screen focus on `Start workout`, decide the route names for the workout flow, and plan how `workoutId`, selected muscle group, and selected exercise will move between screens. |
| 2026-04-28 | Tutor workflow: Home start-workout refactor | Reviewed the saved routed workflow, clarified that the app should use separate screens rather than one page, and refactored the Home screen conceptually around a single `Start Workout` button. Checked the current `index.tsx`, removed the old exercise/muscle-group UI from Home, added `useRouter`, added `isStartingWorkout` and `error` state, wired the button to `handleStartWorkout`, changed the request to `POST /workouts`, read the returned workout object with `response.json()`, used `data.id` as the `workoutId`, and navigated to `/workout/muscle-groups` with route params. | `response` is the HTTP wrapper, while `await response.json()` parses the JSON body into a JavaScript object, so `data.id` can be read normally. `isStartingWorkout` controls both the loading button text and whether the button should be disabled to avoid duplicate workout rows. `{error && <Text>...` conditionally renders an error message only when error state has a value. The Home screen's responsibility is now only to create the workout and pass `workoutId` to the next screen. | Tomorrow, create `apps/mobile/app/workout/muscle-groups.tsx`, read the `workoutId` route param, fetch `GET /exercises`, derive unique muscle groups, render muscle group buttons, and navigate to the exercises screen with `workoutId` plus the selected `muscleGroup`. |
| 2026-04-29 | Tutor workflow: start muscle group screen | Reviewed the current Home screen, confirmed that Home creates a workout and passes `workoutId`, then started `apps/mobile/app/workout/muscle-groups.tsx` and discussed how to read route params with Expo Router. | The muscle group screen receives `workoutId` from Home and will pass both `workoutId` and `muscleGroup` to the exercise screen. `useLocalSearchParams()` is Expo Router's hook for reading the params passed with `router.push`, and the explicit `params.workoutId` form is easier to read while learning. | Next session, add the `Exercise` type and state for `exercises`, `isLoading`, and `error`, then fetch `GET /exercises` in `useEffect` and derive unique muscle groups. |
| 2026-05-01 | Tutor workflow: muscle group screen and start-workout debugging | Continued in `apps/mobile/app/workout/muscle-groups.tsx`, reviewed `useEffect` dependency arrays, added loading and error render states, derived unique muscle groups from fetched exercises, wired muscle group buttons to pass `workoutId` and `muscleGroup` forward, checked the computer's local IP for Expo Go, and debugged why `POST /workouts` failed when the mobile app sent no request body. | `[]` on `useEffect` means run once after the first render; without it, an effect that updates state can fetch repeatedly. Early returns are clearer when loading or error should replace the whole screen. A client cannot read `performedOn` from `req.body` if the request sends no body; for starting a workout now, the backend can insert an empty workout row and let the database default fill `performed_on`. | Build out `apps/mobile/app/workout/exercises.tsx` as the next screen: read `workoutId` and `muscleGroup`, fetch exercises, filter by selected muscle group, and pass the chosen exercise forward to the add-set screen. |
| 2026-05-03 | Tutor workflow: exercise selection, log-set route, and setup docs | Reviewed why `index.tsx` only needs `useRouter`, while screens that receive route data also need `useLocalSearchParams`. Built out `apps/mobile/app/workout/exercises.tsx` to read `workoutId` and `muscleGroup`, fetch exercises, filter by muscle group, and navigate to `/workout/log-set` with `exerciseId`. Created `apps/mobile/app/workout/log-set.tsx`, fetched exercises again, found the chosen exercise by ID, and displayed its name. Debugged an Expo Router "unmatched route" message by restarting Expo with cache clearing. Updated the README with setup notes for `npm install`, Knex migrations/seeds, API/mobile start commands, Expo Go, same Wi-Fi, and local IP setup. | `useRouter()` is for moving to another route; `useLocalSearchParams()` is for reading params passed to the current route. `router.push({ pathname, params })` carries values forward through the flow. A new route file can require an Expo restart with `--clear` before the phone sees it. Repeated fetch/useEffect logic is acceptable for now while learning, but later it can become a shared `useExercises()` hook. `catch { ... }` avoids unused `error` warnings when the caught error object is not needed. For npm scripts, `--` passes extra arguments through to the script, which is why Knex commands use `npm run knex -w apps/api -- migrate:latest`. | Next session, continue in `apps/mobile/app/workout/log-set.tsx`: add fields for reps, load/weight, RIR, and notes, then wire the form to `POST /sets` using the carried `workoutId` and selected `exerciseId`. |
| 2026-05-03 | Tutor workflow: Expo env and network debugging | Asked whether `.env.local` can manage changing API IP addresses, confirmed `apps/mobile/.gitignore` already ignores `.env*.local`, created/used `EXPO_PUBLIC_API_URL`, cleared Expo cache, installed the Expo tunnel helper, and diagnosed why the app loaded but `Start Workout` failed from cafe Wi-Fi. | Expo public env vars need the `EXPO_PUBLIC_` prefix and are suitable for local non-secret values like an API base URL. `npm run start -w apps/mobile -- --clear` is the correct mobile command because the app has a `start` script, not `dev`. Expo tunnel mode helps Expo Go download the app bundle, but it does not expose the local Express API. Cafe Wi-Fi can block the phone from reaching the computer's `192.168.x.x:3001` address even when the computer can `curl` the API locally. | At home, start the API first with `npm run dev -w apps/api`, then start Expo. If using LAN, confirm the phone can open `http://<computer-ip>:3001/exercises`; if not, use a phone hotspot or an API tunnel before continuing with the log-set form. |
| 2026-05-03 | Tutor workflow: plan `POST /sets` request shape | Started from the existing backend `addSet` function and `SetData` type, then planned what the mobile `log-set` screen should send to `POST /sets`. Reviewed the in-progress `log-set.tsx` file and discussed which values come from route params versus form inputs. | `workoutId` and `exerciseId` come from route params. `reps`, `load`, `rir`, and `note` come from form inputs. `setNumber` can be app-generated for now, with `1` as the simplest first version. Saving a set should happen in `handleSaveSet`, not `useEffect`, because it is triggered by a user button press. React Native forms use `TextInput`, and text input values should be stored as strings before converting required numbers and blank optional fields into `null` for the API request. | Continue in `apps/mobile/app/workout/log-set.tsx`: import `TextInput`, add input state, render the reps/load/RIR/note fields, add a save button, then build the request body for `POST /sets`. |
| 2026-05-04 | Tutor workflow: log-set save handler reminder | Asked whether saving a set should use `useEffect` or a handler, then reviewed the current `apps/mobile/app/workout/log-set.tsx` implementation for `handleSaveSet`, `requestBody`, and the Save button. | Saving belongs in `handleSaveSet`, not `useEffect`, because it should only run after the user taps Save. `body: JSON.stringify(requestBody)` is how the frontend sends the JSON payload that Express reads as `req.body`. The handler is mostly in the right shape, but the Save `Pressable` still needs `onPress={handleSaveSet}`. The current `isLoading` state is shared between loading exercises and saving, so a later improvement could split that into `isLoading` and `isSaving`. | Tomorrow, add `onPress={handleSaveSet}` to the Save button, test `POST /sets`, then decide whether a successful save should clear the form, navigate back, or show a saved message. Be ready to explain what `body: JSON.stringify(requestBody)` does. |
| 2026-05-05 | Tutor workflow: checkpoint before stopping | Activated the PromptKit tutor workflow, checked that the root briefing already included PromptKit guidance, reviewed the saved notes from 2026-05-04, and confirmed what had been captured from the previous session. | The previous save-handler explanation was already recorded as a summary in `learning-plan.md` and `progress-journal.md`. The exact wording from the previous response is not available in the repo notes, but the important learning point and next action are saved. | Resume in `apps/mobile/app/workout/log-set.tsx` by wiring the Save `Pressable` to `handleSaveSet`, then test `POST /sets` and choose the success behaviour. |
| 2026-05-07 | Tutor workflow: dev scripts and form labels | Discussed how to avoid typing the long API and mobile commands, tried a combined `concurrently` workflow, decided separate terminals are better for seeing the Expo QR code, and reviewed why the Reps label stayed above the input. | Root scripts can shorten workspace commands, while separate terminal panes keep the API logs and Expo QR code readable. In React Native, row layout belongs on the wrapper `View`, not the `Text` label. `gap` controls spacing between row children, and `flex: 1` should be applied through a row-only input style rather than the shared input style if other inputs are still in a vertical container. | Continue in `apps/mobile/app/workout/log-set.tsx`: put each field in a label-plus-input row, use a separate row input style when the input needs to fill remaining width, then test the Save button and decide what happens after a successful set save. |
| 2026-05-08 | Tutor workflow: filtered sets route and display plan | Fixed the label/input row approach, planned a filtered `GET /sets` route using `workoutId` and `exerciseId`, added `getSetsByWorkoutAndExercise`, removed the need for `getAllSets`, and reviewed the mobile `loadSets` plan. | `GET /sets` can require query params for this app flow. Query params arrive as strings, so the route should check for missing values, convert them with `Number(...)`, and use `Number.isNaN(...)` to reject invalid IDs before querying the database. In `log-set.tsx`, `loadSets` should sit outside `useEffect` if it needs to be called on initial load and again after a successful save. | Resume in `apps/mobile/app/workout/log-set.tsx`: move `loadSets` outside `useEffect`, fetch `/sets?workoutId=${workoutId}&exerciseId=${exerciseId}`, call `loadSets()` inside `useEffect`, call it again after `POST /sets` succeeds, then render `sets` below the Save button. |
| 2026-05-10 | Tutor workflow: saved sets table and input steppers | Moved through the `log-set.tsx` save/display flow: placed `loadSets` where both `useEffect` and `handleSaveSet` can call it, clarified why `POST /sets` uses a JSON body while filtered `GET /sets` uses query params, added a reminder comment for the `useEffect` dependency warning, rendered saved sets in a table-like layout, calculated `setNumber` from the current saved sets, and started plus/minus controls for the reps input. | `loadSets` can be shared by screen-load and save-success behavior when it lives inside the screen component but outside `useEffect`. The hooks dependency warning is about functions and route params used by the effect, not a broken runtime flow. React Native table-like UI is built from `View` rows and repeated `Text` cells, not HTML table tags. Plus/minus controls should only update `TextInput` state; wrapping calls in `() => ...` prevents them from running during render. | Next session, finish the stepper controls for reps, load, and RIR. Add an optional max argument to `changeNumberValue` so RIR is capped at 10, wire the minus buttons, and confirm that changing inputs does not save until the Save button is pressed. |
| 2026-05-11 | Tutor workflow: local IP env script and update-set planning | Created `scripts/update-mobile-env.mjs`, connected it to the root `dev:mobile` script, confirmed it can create or update `apps/mobile/.env.local`, discussed README wording, then planned long-press saved-set editing with Update and Delete actions. Reviewed the first `updateSetById` DB query function and the spread operator. | `.env.local` stores finished text values and cannot import JavaScript variables. The Node script runs `ipconfig getifaddr en0`, builds the API URL, checks whether the env file exists, replaces the `EXPO_PUBLIC_API_URL` line, and writes the file before Expo starts. The spread operator copies object properties, but it only works safely for DB updates when the object keys match the database columns. Since `SetData` uses camelCase and the database uses snake_case, update queries should map fields explicitly. Because `updateSetById` receives the `id` separately, its update payload should be `SetData`, not `Set`. | Continue in `apps/api/server/db/sets.ts`: change `updateSetById` to accept `SetData`, then add `PATCH /sets/:id` and `DELETE /sets/:id` routes. After the backend is tested, wire `selectedSetId`, `onLongPress`, and conditional Save versus Update/Delete buttons in `apps/mobile/app/workout/log-set.tsx`. |
| 2026-05-17 | Tutor workflow: route params recap and delete route review | Recapped the last sessions after losing consistency, reviewed `apps/mobile/app/workout/exercises.tsx` route params, checked `deleteSetById` in `apps/api/server/db/sets.ts`, and reviewed the new `DELETE /sets/:id` route in `apps/api/server/routes/sets.ts`. | `workoutId` and `muscleGroup` are carried forward from previous screens, while `exerciseId` is selected on the exercises screen from the mapped API data. `String(exercise.id)` converts the existing numeric id into a route-param string. Knex `.delete()` returns the number of rows deleted, so `deleteSetById` should return `Promise<number>`. The delete route should validate the id before calling the DB, return `404` when no row was deleted, and return `204 No Content` for a successful delete. A frontend should not parse JSON from a `204` response. | Tomorrow, test the backend `PATCH /sets/:id` and `DELETE /sets/:id` routes, then continue wiring mobile long-press edit mode in `apps/mobile/app/workout/log-set.tsx` with selected set state and conditional Save/Update/Delete actions. |
| 2026-05-18 | Tutor workflow: tested PATCH and DELETE sets routes | Tested `PATCH /sets/:id` and `DELETE /sets/:id` in Postman. | The backend update/delete routes work through the API before wiring them into mobile. `204 No Content` means a successful delete has no response body to parse. | Start mobile long-press edit mode in `apps/mobile/app/workout/log-set.tsx`. |
| 2026-05-19 | Tutor workflow: plan `log-set.tsx` component extraction | Reviewed that `log-set.tsx` has grown to 341 lines, researched Expo Router file organization, created `apps/mobile/components/workout`, and planned the first extracted component: `NumberStepperInput`. Discussed parent/child components, props, controlled `TextInput` values, callback props, and why `LogSetScreen` should still own state and behavior. | Files inside `app/` are routes in Expo Router, so reusable UI should live outside `app/`, such as `components/workout`. A component is a function that returns UI. The parent is the component rendering another component, and the child is the component being rendered. Props are values and callbacks passed from parent to child. `NumberStepperInputProps` is the TypeScript contract for those props. `value` is the current text in the input, `onChangeText` updates parent state, and `placeholder` is only the grey hint when the value is empty. | Next session, create `apps/mobile/components/workout/NumberStepperInput.tsx`, move only the Reps row into it first, import it into `log-set.tsx`, replace the Reps JSX with `<NumberStepperInput ... />`, then check that Reps still displays and its plus/minus buttons work before replacing Load and RIR. |
| 2026-05-20 | Tutor workflow: extracted `NumberStepperInput` and reviewed callback props | Created and tested `NumberStepperInput` for Reps first, then applied it to Load and RIR. Asked about the `@/` import alias, whether it can replace `../../../api/...`, what `keyboardType="numeric"` guarantees, which styles can be removed after extraction, and why `onIncrease`/`onDecrease` are passed as props instead of calling `changeNumberValue` directly inside the child component. | `@/` points to the `apps/mobile` root, not the repo root, so it works for mobile components but not API server model imports. `keyboardType="numeric"` shows a numeric keyboard but does not fully validate input. `NumberStepperInput` should own the repeated row layout, while `LogSetScreen` owns state and behavior. Callback props let the parent decide the field-specific behavior, such as RIR using half steps and a max of 10. After the extraction, `incrementButton` and `incrementText` can be removed from `log-set.tsx`, and unused `rowInput` can be removed from `NumberStepperInput.tsx`. | Clean up the unused styles, then continue mobile long-press edit mode in `apps/mobile/app/workout/log-set.tsx`: add selected set state, populate the form on long press, and show Update/Delete instead of Save when editing. |
| 2026-05-23 | Tutor workflow: continue `log-set.tsx` refactor planning | Reviewed where the project left off, checked the new `SavedSetsTable.tsx`, confirmed the old table block needed to be replaced with `<SavedSetsTable sets={sets} />`, and planned what else could be extracted from `log-set.tsx` before adding long-press update/delete. | `SavedSetsTable` is a good component because it only needs the `sets` prop and can own the table styles. The next useful extraction is `SetForm`, which would render the Reps, Load, RIR, Notes, and Save button UI. `LogSetScreen` should still own the form state and save behavior for now, while `SetForm` receives values and callbacks as props. It is normal for `SetForm` to render `NumberStepperInput`; that creates a clear component hierarchy rather than a problem. | Create `apps/mobile/components/workout/SetForm.tsx`, move the form UI into it, import `NumberStepperInput` inside `SetForm`, pass values and callbacks from `log-set.tsx`, then test that saving still works before adding long-press edit mode. |
| 2026-05-24 | Tutor workflow: extracted `SetForm` | Created `apps/mobile/components/workout/SetForm.tsx` step by step, moved the Reps, Load, RIR, Notes, and Save button UI into it, passed values and actions from `log-set.tsx`, restored the chosen exercise title in the screen, and cleaned up form-only imports and styles from `log-set.tsx`. | Props can be grouped into values and actions. `SetForm` receives values like `reps`, `load`, `rir`, `note`, and `isSaving`, plus action props like `onChangeReps`, `onIncreaseLoad`, `onDecreaseRir`, and `onSave`. `LogSetScreen` should keep screen-level responsibilities: route params, selected exercise title, state, save behavior, and saved sets. Reusable form styles belong in `SetForm`, while screen styles such as `container`, `title`, and `errorText` stay in `log-set.tsx`. | Run the app or TypeScript check to confirm the refactor still works, then continue mobile long-press edit mode: add selected set state, populate the form on long press, and show Update/Delete actions instead of Save. |
| 2026-05-24 | Tutor workflow: long-press selected set flow | Updated `SavedSetsTable` so each saved set row can be wrapped in a `Pressable` and call an `onLongPressSet` callback with the current `SetDisplay` from the `map`. Discussed why `(set: SetDisplay) => void` means a callback receives one selected set and returns nothing useful, and why `onLongPress={() => props.onLongPressSet(set)}` delays the call until the row is actually long-pressed. | Callback props let a child component report events back to the parent: `SavedSetsTable` knows which row was pressed, while `LogSetScreen` owns the selected-set state and form state. The UX decision is that long press should only select the row and reveal Edit/Delete choices; the form should not populate until the user chooses Edit. `selectedSetId` should start as `null`, then become the selected set's id after long press. | Tomorrow, continue in `apps/mobile/app/workout/log-set.tsx`: add `selectedSetId`, pass `handleLongPressSet` into `SavedSetsTable`, make long press store only the selected id, then add a separate Edit action that finds the selected set and fills the form. |
| 2026-05-25 | Tutor workflow: verified long-press selection and planned haptics | Checked the latest `log-set.tsx` and `SavedSetsTable.tsx`, confirmed `handleLongPressSet` receives a `SetDisplay`, verified long press selection works, and discussed adding a small phone vibration when a set is selected. | The callback chain is now working: `SavedSetsTable` maps over saved sets, each row calls `onLongPressSet(set)`, and `LogSetScreen` stores `selectedSetId` from the selected row. Passing the whole `SetDisplay` is useful because later Edit can reuse fields like `reps`, `load`, `rir`, and `note`. `expo-haptics` is already installed, and `Haptics.selectionAsync()` is the clearest fit for subtle row selection feedback. | Tomorrow, import `expo-haptics` in `apps/mobile/app/workout/log-set.tsx`, call `Haptics.selectionAsync()` inside `handleLongPressSet`, then continue with visible selected-row feedback and separate Edit/Delete actions. |
| 2026-05-26 | Reflect workflow: edit-mode UI for saved sets | Reviewed the saved-set long-press flow, changed the UX decision so long-press immediately populates the form, updated `SetForm` so Save is replaced by Update/Delete while editing, derived edit mode from `selectedSetId`, and started passing `selectedSetId` into `SavedSetsTable` for selected-row styling. | I learned that `selectedSetId` can be the single source of truth for edit mode: when it is `null`, the form is saving a new set, and when it has an id, the form is editing that selected set. I also learned that parent state can be passed down as a prop so a child component can style the matching row, and that conditional React Native styles need an array like `[styles.tableRow, isSelected && styles.selectedRow]`, not parentheses with a comma. | Tomorrow, fix/check the selected-row style in `SavedSetsTable`, then wire `handleUpdateSet` to `PATCH /sets/:id` before adding delete behavior. |
| 2026-05-27 | Tutor workflow: mobile update set flow | Reviewed `apps/mobile/app/workout/log-set.tsx`, checked the selected-row styling, built and tested `handleUpdateSet`, and discussed how to deselect a set after entering edit mode. | The PATCH request needs to match the backend's current `SetData` shape, so it sends `workoutId`, `exerciseId`, the selected set's existing `setNumber`, and the form values for `reps`, `load`, `rir`, and `note`. `selectedSetId` belongs in the URL as `/sets/:id`, and `response.ok` should be checked before reloading sets and clearing edit mode. A visible Cancel button is a simpler next step than intercepting the Android hardware back button. | Add a Cancel button to `SetForm` edit mode, pass an `onCancel` callback from `log-set.tsx`, clear `selectedSetId` and the form fields in `handleCancelEdit`, then continue with `handleDeleteSet`. |
| 2026-05-28 | Tutor workflow: mutations, delete handler, and set-number resequencing | Asked why the app uses `PATCH` and a handler function instead of a bootcamp-style mutation, then reviewed `handleDeleteSet`, `SetForm`, `SavedSetsTable`, and the existing backend `DELETE /sets/:id` route. Added/checked a Cancel action in edit mode and shaped the mobile delete handler so it sends `DELETE` with no request body, checks `response.ok`, reloads sets, and exits edit mode. Discussed what should happen to set numbers after deleting a middle set. | `PATCH` is the REST API method that updates the server, while a client-side mutation hook would usually wrap that same `fetch` request and manage loading/error/cache behavior. `DELETE /sets/:id` does not need a JSON body or `Content-Type` header, and the frontend should not parse JSON from a `204 No Content` response. The current delete flow removes the row, but deleting set 2 from sets 1, 2, 3 can leave set numbers 1 and 3 unless the app resequences them. The better long-term fix is backend resequencing after delete for the same `workoutId` and `exerciseId`. | Tomorrow, test the mobile delete flow, call `handleCancelEdit()` after a successful delete to clear the form, then add backend resequencing: find the set before deleting it, remember its workout/exercise ids, delete it, reload remaining sets for that workout/exercise ordered by set number, and update their `set_number` values to 1, 2, 3. |
| 2026-05-29 | Tutor workflow: resume delete resequencing | Refreshed the previous session, checked the backend and frontend delete paths, confirmed the mobile delete handler does not call `response.json()` after `DELETE`, then started backend resequencing in `apps/api/server/db/sets.ts`. | The frontend delete path is already shaped correctly for a `204 No Content` response. Resequencing belongs in the backend DB layer because the database knows which saved set belongs to which `workout_id` and `exercise_id`. The delete function now starts by finding the set before deleting it and returning `0` if it is missing. The next fix is to `await` the delete query, loop through `remainingSets`, update each row's `set_number` to `index + 1`, and return `deletedCount`. | Resume in `apps/api/server/db/sets.ts`: add `await` to `const deletedCount = await db('sets').where('id', id).delete();`, then loop through `remainingSets` and update each remaining row's `set_number`. After that, test by deleting a middle set and confirming the displayed set numbers close the gap. |
| 2026-05-30 | Tutor workflow: finish delete resequencing and identify GET ordering | Reviewed `deleteSetById` line by line, completed and tested the loop that updates each remaining set's `set_number` to `index + 1`, and discussed why `getSetsByWorkoutAndExercise` still needs explicit ordering. | `deleteSetById` now follows the right sequence: find the set first, return `0` if missing, delete it, fetch remaining sets for the same `workout_id` and `exercise_id`, update each remaining row by id, and return `deletedCount`. Resequencing fixes the stored numbers, but `GET /sets` also needs `.orderBy('set_number', 'asc')` because SQL does not guarantee display order unless the query asks for it. | Tomorrow, add `.orderBy('set_number', 'asc')` to `getSetsByWorkoutAndExercise` in `apps/api/server/db/sets.ts`, then test that saved sets still display as 1, 2, 3 after creating, updating, and deleting sets. |
| 2026-05-31 | Tutor workflow: start previous-workout history on Home | Confirmed ordered set retrieval was complete, added `getWorkouts()` with newest-first ordering, added `GET /workouts`, clarified `WorkoutData` versus the saved `Workout` model, removed the unused `addWorkout` parameter, loaded workout history from the Home screen with `useEffect`, defined a local mobile `Workout` interface, and temporarily mapped the returned workouts to verify the data path. | I learned that an async DB function returning one inserted row should use `Promise<Workout>`, while a query returning multiple rows should use `Promise<Workout[]>`. Database-generated `createdAt` belongs on the saved `Workout` model rather than the client-supplied creation type. On mobile, `useEffect` is appropriate for fetching the list after the first render, and the mobile screen should avoid importing backend server models directly. | Tomorrow, refactor the temporary Home map into a previous-workouts section: add the missing row style, format the date, weekday, and start time, improve the list layout, and decide how the history should refresh when returning to Home after starting a workout. |
| 2026-06-02 | Tutor workflow: format and style previous-workout cards | Converted SQLite UTC timestamp strings into JavaScript `Date` objects in the Home workout list, rendered a localized date, weekday, and time, and reviewed card spacing, shadows, top padding, Flexbox defaults, and the next scrolling step. | I learned that API timestamps arrive as strings, so date methods cannot be called on them directly. Replacing the SQLite timestamp's space with `T` and appending `Z` produces an explicit UTC timestamp that `new Date(...)` can parse reliably. A React Native `View` uses Flexbox by default, `gap` affects direct children only, list cards should size themselves without `flex: 1`, and a `ScrollView` should use `contentContainerStyle` for inner spacing. | Tomorrow, wrap the Home screen content in a `ScrollView`, keep the spacing styles on `contentContainerStyle`, confirm the workout cards scroll correctly, and decide how the workout history reloads when returning to Home. |
| 2026-06-03 | Tutor workflow: plan clickable workout detail page | Discussed the next feature after displaying previous workouts on Home: make each workout clickable and create a workout detail page that shows all exercises performed in that workout with their sets. Saved the detailed reminder in `promptkit/notes/session-memory.md`. | The app uses Expo Router. The missing backend idea is an endpoint that gets all sets for one workout, joined with exercise names. A flat API response is the simplest next shape, because it is close to the existing `SetDisplay` model and the frontend can group rows by exercise. | Tomorrow, reread `promptkit/notes/session-memory.md`, then start by adding a DB query for all sets in one workout, followed by a route like `GET /workouts/:id/sets`. |
| 2026-06-04 | Tutor workflow: start workout detail endpoint | Reviewed the saved workout detail plan, clarified flat list versus grouped data, confirmed `SetDisplay[]` is the right response shape, moved the all-sets-for-one-workout DB function into `apps/api/server/db/sets.ts`, and added the route shape in `apps/api/server/routes/workouts.ts`. | A flat list means each row is one set at the same array level; the mobile screen can group those rows by `exerciseName` later. `getSetsByWorkoutId` needs joins because `exerciseName` lives in `exercises` and `performedOn` lives in `workouts`. The route belongs under `GET /workouts/:id/sets`, while the query belongs in `db/sets.ts`. Ordering by `exerciseName` then `setNumber` groups exercise rows together and sorts each exercise's sets in order; the current schema does not yet store the actual exercise sequence from the workout. | Fix the route error wording from `set id` to `workout id`, test `GET /workouts/:id/sets` with an existing workout id, then start the mobile workout detail screen and group the returned `SetDisplay[]` by exercise name. |
| 2026-06-05 | Tutor workflow: dynamic route and workout detail screen start | Fixed the workout route validation wording, discussed Expo Router dynamic route filenames, created `apps/mobile/app/workout/[workoutId].tsx`, read `workoutId` with `useLocalSearchParams`, and started the `useEffect` fetch shape for the selected workout's sets. | Square brackets in an Expo Router filename mark a dynamic URL segment, so `[workoutId].tsx` matches paths like `/workout/12`. This page should fetch the existing flat `GET /workouts/:id/sets` response and group rows by `exerciseName`; it does not need to fetch all exercises again because the backend join already returns exercise names. Grouping is different from filtering: filtering keeps/removes rows, while grouping builds sections from rows that share a key. | Tomorrow, fix the detail fetch URL to `/workouts/${workoutId}/sets`, fix the typo in the error message, add loading/error/normal render returns, use `[workoutId]` as the effect dependency, then group the returned `SetDisplay[]` by `exerciseName` and make Home workout cards navigate to `/workout/[workoutId]`. |
| 2026-06-08 | Tutor workflow: workout detail grouping with `reduce` | Reviewed the updated workout detail screen. Confirmed the fetch URL now uses `/workouts/${workoutId}/sets`, the error typo is fixed, `[workoutId]` is in the effect dependency array, loading/error branches and styles exist, and a `reduce` grouping attempt has been added. Then discussed `reduce`, the accumulator, `currentSet`, and the `Record<string, SetDisplay[]>` type in detail. | `reduce` builds one final value from many array items. In this screen, `groups` is the accumulator object being built, `currentSet` is one `SetDisplay` row from `workoutSets`, and `groups[exerciseName]` is the bucket for that exercise. `Record<string, SetDisplay[]>` means an object whose keys are strings such as exercise names and whose values are arrays of `SetDisplay` rows. The first type argument is the key type, and the second is the value type. | Tomorrow, continue in `apps/mobile/app/workout/[workoutId].tsx`: render `Object.entries(groupedSets)` after the loading/error returns, first showing exercise headings, then nested set rows. After that, make Home previous-workout cards navigate to `/workout/[workoutId]`. |
| 2026-06-09 | Tutor workflow: previous workout cards and read-only detail page | Wired the Home previous-workout card idea with `Pressable` and `router.push` to `/workout/[workoutId]`, fixed the route pathname leading slash, debugged the black detail screen caused by the final return missing `styles.container`, added/understood the empty-state fallback for workouts with no sets, reused `SavedSetsTable` under each grouped exercise heading, reviewed why shadow styles need `backgroundColor`, `shadowOpacity`, `shadowRadius`, and Android `elevation`, and decided the previous workout detail page should stay read-only for now. | Expo Router dynamic paths should use the app-root pathname such as `/workout/[workoutId]`, and route params should send the numeric id as a string. `Object.entries(groupedSets)` gives `[exerciseName, sets]` pairs, which makes it natural for `[workoutId].tsx` to own the exercise heading and let `SavedSetsTable` own only the set rows. If the API returns `[]`, loading and error are both false but the grouped object is empty, so an explicit `workoutSets.length === 0` branch prevents a blank screen. Editing sets belongs in `log-set.tsx`; later the detail page can navigate to `log-set.tsx` for a specific workout/exercise once the API response includes `exerciseId`. | Tomorrow, polish `apps/mobile/app/workout/[workoutId].tsx`: add a header showing day of week, date, and created/start time like the Home workout card. The quickest approach is to pass `performedOn` and `createdAt` from `index.tsx` as route params; the later cleaner approach is a backend `GET /workouts/:id` endpoint for loading workout header data directly. |
| 2026-06-10 | Tutor workflow: Home refresh and previous-workout delete planning | Replaced the Home screen's one-time workout loading idea with the concept of reloading on screen focus using `useFocusEffect` and `useCallback`, then planned long-press selection and deletion for previous-workout cards. Reviewed the `deleteWorkoutById` DB function shape and checked the started `handleLongPressWorkout` naming on `index.tsx`. | `useEffect(..., [])` only runs when Home first mounts, while `useFocusEffect` runs when Home becomes visible again, which is why it fixes the missing-new-workout problem after returning from the workout flow. Long press should select a workout card only; normal press should keep navigating to the detail page. Deleting a workout should delete child `sets` first, then the parent `workouts` row, and the mobile handler should not parse JSON from a successful `204 No Content` delete response. | Tomorrow, continue in `apps/mobile/app/(tabs)/index.tsx`: make `handleLongPressWorkout(workout)` set `selectedWorkoutId`, add selected-card styling plus arrow/delete icons, then add `handleDeleteWorkout(workoutId)` with an `Alert` confirmation, `DELETE /workouts/:id`, `response.ok` checking, `loadWorkouts()`, and clearing `selectedWorkoutId`. |
| 2026-06-11 | Tutor workflow: Home workout-card selection UI | Continued in `apps/mobile/app/(tabs)/index.tsx`, reviewed why `handleLongPressWorkout(workout)` receives the current mapped workout, added/checked `selectedWorkoutId`, calculated `isSelected` with `selectedWorkoutId === workout.id`, split the previous-workout card into left text and right action areas, added `Ionicons` for chevron/trash states, and added haptic feedback on long press. | `isSelected` is an exact boolean that is true only for the card whose `workout.id` matches `selectedWorkoutId`. `onLongPress={() => handleLongPressWorkout(workout)}` waits for the long press and then passes the current card's workout into the handler. `@expo/vector-icons` is already installed, so `Ionicons` can render `chevron-forward` for normal cards and `trash-outline` for selected cards. The shadow glitch after moving selection is likely caused by adding/removing `borderWidth`; keeping `borderWidth: 1` and `borderColor: 'transparent'` on the base card, then only changing `borderColor` when selected, keeps the card dimensions stable. | Tomorrow, update `prevWorkoutContainer` so it always has `borderWidth: 1` and `borderColor: 'transparent'`, change `selectedWorkoutContainer` to only set the red `borderColor`, then wire the trash icon to a separate `handleDeleteWorkout(workoutId)` with `Alert`, `DELETE /workouts/:id`, `response.ok`, `loadWorkouts()`, and `setSelectedWorkoutId(null)`. |
| 2026-06-12 | Tutor workflow: custom previous-workout delete modal | Checked `handleDeleteWorkout`, fixed the endpoint concept from `/sets/:id` to `/workouts/:id`, confirmed that a successful `204 No Content` delete should not use `response.json()`, then planned and reviewed a custom confirmation modal instead of the native `Alert`. Added modal visibility state, cancel/delete behavior, styled backdrop/card/buttons, and verified the delete flow works. | A custom `Modal` is screen-level UI, so it should not live inside `workouts.map(...)`; putting it inside the map would create one modal per workout card. `Modal transparent` lets the screen render its own dim backdrop. The trash icon should open the modal, Cancel should close it and clear `selectedWorkoutId`, and Delete should call `handleDeleteWorkout`. After a successful delete, the app should reload workouts, clear selection, and close the modal. | Next session, move the modal outside the `ScrollView` by wrapping the screen return in a fragment, so the `ScrollView` and `Modal` are siblings. Then retest long press, trash press, Cancel, Delete, and normal card navigation. |
| 2026-06-13 | Tutor workflow: workout detail edit navigation and exercise deletion planning | Reviewed the Home selection-mode behaviour, updated the workout detail flow so exercise sections and table rows can navigate to `log-set.tsx`, added `exerciseId` to `SetDisplay`/`columnsSetDisplay`, discussed `sets[0].exerciseId`, switched the detail reload concept to `useFocusEffect`, styled the exercise section title row with a chevron, and planned deleting one exercise's sets from a workout. Started the backend DB function `deleteSetsByWorkoutAndExercise(workoutId, exerciseId)`. | Selection mode should not accidentally navigate while something is selected; normal taps can select/deselect within that mode. `onPress` must receive a function, not the result of calling `router.push`. `exerciseId` should come from grouped set data, not `[workoutId].tsx` route params. `SavedSetsTable` needed optional `onPressSet` because inner row `Pressable`s catch row taps. `useFocusEffect` reloads when returning to a screen, while a `useEffect` dependency only reruns when the dependency value changes. Deleting an exercise from a workout means deleting matching `sets` rows, not the exercise catalogue row. | Add the route in `apps/api/server/routes/workouts.ts` for `DELETE /workouts/:workoutId/exercises/:exerciseId/sets`, validate both IDs, call `deleteSetsByWorkoutAndExercise`, and return `400`/`404`/`204` as appropriate. Then add selected exercise section state, trash icon, confirmation modal, delete handler, and reload in `apps/mobile/app/workout/[workoutId].tsx`. |
| 2026-06-14 | Tutor workflow: exercise-section delete and nested pressables | Checked the new `DELETE /workouts/:workoutId/exercises/:exerciseId/sets` route, confirmed that `/workouts/12/exercises/1/sets` is the right URL shape, then reviewed the in-progress exercise-section selection code in `apps/mobile/app/workout/[workoutId].tsx`. Added/understood the need for `selectedExerciseId`, `selectedExerciseName`, `isDeleteModalVisible`, `handleLongPressExercise`, and `handlePressExercise`. Debugged why long-press works around the saved sets table but not inside it. | The backend route deletes matching `sets` rows for one workout/exercise pair and does not delete the exercise catalogue row. On mobile, the saved sets table has inner row `Pressable`s, so row taps do not bubble to the outer exercise-section `Pressable`. Passing `onLongPressSet={() => {}}` means long-pressing a table row is handled but intentionally does nothing. The fix is to make table-row press and long-press delegate to the same exercise-level handlers. `handlePressExercise` also needs to return before navigation when selection mode is active. | Tomorrow, resume in `[workoutId].tsx`: fix `handlePressExercise` so selection mode only selects/deselects and returns; pass `onLongPressSet={() => handleLongPressExercise(exerciseId, exerciseName)}` and `onPressSet={() => handlePressExercise(exerciseId, exerciseName)}` into `SavedSetsTable`; then add the delete confirmation modal and call `DELETE /workouts/${workoutId}/exercises/${selectedExerciseId}/sets` on confirm. |
| 2026-06-15 | Tutor workflow: saved-set tap selection mode | Reviewed the updated saved-set edit flow in `apps/mobile/app/workout/log-set.tsx`. Added a `selectSetForEditing(selectedSet)` helper, reused it from `handleLongPressSet`, and added `handlePressSet(selectedSet)` so tapping rows during edit mode can either cancel the current selection or move selection to another set. | The parent screen should own selection behaviour because it owns `selectedSetId` and the form fields. `SavedSetsTable` should stay reusable and only report row events through callback props. A helper function avoids duplicating the form-populating logic across long press and tap handlers. Tapping the same selected set should call the full cancel path so the form clears, while tapping a different set should refill the form from that row. | Tomorrow, confirm `onPressSet={handlePressSet}` is passed into `SavedSetsTable`, test long press, same-row tap cancel, and different-row tap selection. Then consider whether update success should call `handleCancelEdit()` so the form clears after saving changes. |
| 2026-06-16 | Tutor workflow: add exercise from workout detail | Planned and reviewed adding an `Add exercise` action at the top of `apps/mobile/app/workout/[workoutId].tsx`. The button navigates to `/workout/muscle-groups` with the existing `workoutId`, so the user can pick a muscle group, pick an exercise, and add sets to the same workout. Moved the empty workout message into the main return with a ternary so the button can remain visible, and reviewed why the new fixed-header plus `ScrollView` layout changed the exercise-card shadow spacing. | Adding an exercise from an existing workout should not call `POST /workouts`; the existing route param already identifies the workout. A fixed top button belongs outside the `ScrollView` but inside the screen container, while the scroll content owns spacing for the exercise cards. Empty state should be content inside the scroll area, not an early return, when the screen action still needs to stay visible. Selection state pairs like `selectedExerciseId` and `selectedExerciseName` must be updated together so the delete modal text matches the id being deleted. | Tomorrow, test the workout detail screen end to end: Add exercise from a previous workout, add sets, return to detail, and confirm `useFocusEffect` reloads. Then clean up delete selection after success by also clearing `selectedExerciseName`, and consider adding `style={styles.exerciseList}` with `flex: 1` to the `ScrollView` if the layout needs clearer remaining-space behaviour. |
| 2026-06-17 | Tutor workflow: deploy API for portfolio preview | Planned a deployment strategy for a job-search portfolio app, chose Render for the first API deployment, and deployed the Express/Knex/SQLite API from `feature/deploy-api`. Fixed deployment blockers step by step: changed the API to use `process.env.PORT || 3001`, configured Render root directory as `apps/api`, used `NODE_VERSION=22`, kept `NODE_ENV=production`, installed dev dependencies so `tsx` is available, rebuilt `sqlite3` from source to avoid the GLIBC error, changed production SQLite to a deployable `prod.sqlite3` path, added production migration/seed paths, and added a deploy script that runs migrations, seeds, then starts the API. Confirmed `https://workout-log-api-oiof.onrender.com/exercises` returns JSON. | A deployed mobile app needs a public API URL; Expo Go/local IP workflow is separate from preview/production builds. Render injects its own `PORT`, so hardcoded `3001` is not enough for deployment. `NODE_ENV=production` changes which Knex config is used and can also affect dependency installation. Native packages such as `sqlite3` can fail on hosted Linux environments unless rebuilt for that environment. A long-lived deployment branch is not necessary; short-lived branches plus environment variables are cleaner. | Resume by merging `feature/deploy-api` into `main`, then update the Render service branch from `feature/deploy-api` to `main` and confirm `/exercises` still returns JSON. After that, create `feature/eas-preview` from `main` and start the EAS Android preview build using `EXPO_PUBLIC_API_URL=https://workout-log-api-oiof.onrender.com`. |
| 2026-06-18 | Tutor workflow: EAS Android preview build | Confirmed Render now deploys from `main` and `/exercises` works, deleted the need for the old API deployment branch, created/used `feature/eas-preview`, set up an Expo account, ran EAS configuration, renamed the app to `Grind Notes`, fixed the slug/project mismatch by relinking the EAS project as `grind-notes`, configured the preview build to use the Render API URL, generated an Android keystore through Expo, and built/install-tested the Android APK successfully. | EAS Build creates a cloud-built APK for testing outside Expo Go. The app reads `process.env.EXPO_PUBLIC_API_URL`; local development gets that value from `.env.local`, while the EAS `preview` profile gets it from `eas.json`. The Android package id is a technical identifier (`com.ale2502.grindnotes`), while the display name is `Grind Notes` and the Expo slug/project name is `grind-notes`. `distribution: internal` means preview/testing, and `android.buildType: apk` makes the build easy to install directly on an Android phone. | Tomorrow, review and commit the EAS preview changes, decide whether to merge `feature/eas-preview` into `main`, and continue APK smoke testing against the deployed Render API. |
| 2026-06-19 | Tutor workflow: recruiter-friendly web demo and CORS | Reframed the portfolio goal from an Android APK preview to a low-friction browser demo for recruiters. Temporarily pointed `apps/mobile/.env.local` at the Render API to test Expo Web, confirmed the web app rendered, diagnosed the `Start Workout` failure as a browser CORS block, added CORS support to the Express API on `cors-for-web-demo`, merged it, and confirmed the local Expo Web app works against `https://workout-log-api-oiof.onrender.com`. | A mobile APK can call the API without browser CORS checks, but Expo Web runs in Chrome and needs the API to send CORS headers. Because this project owns the Express API, enabling CORS directly with middleware is simpler than creating a proxy server. `scripts/update-mobile-env.mjs` should keep managing `.env.local` for local mobile development, while the future deployed web host should provide `EXPO_PUBLIC_API_URL` through its environment settings. | Tomorrow, keep local mobile development in mind and move to static web deployment: run an Expo web export/build with the Render API URL, deploy the `apps/mobile/dist` output to a static host, set `EXPO_PUBLIC_API_URL=https://workout-log-api-oiof.onrender.com` in the host environment, test the live URL, then update the README/portfolio with the web demo link first and the APK link second. |
| 2026-06-21 | Tutor workflow: export static Expo Web site | Refreshed the static deployment model, confirmed `.env.local` contained the live Render API URL, ran `npx expo export --platform web`, and locally served and tested the generated `apps/mobile/dist` site. | A static export is browser-ready generated output, and Expo embeds `EXPO_PUBLIC_` values at build time. The successful export produced 11 static routes. `apps/mobile/dist/` is already ignored and should not be committed because Render can reproduce it during each deployment. | Tomorrow, create the Render Static Site, configure its build and publish settings plus `EXPO_PUBLIC_API_URL`, deploy it, and smoke-test normal navigation and direct route refreshes on the live URL. |
| 2026-06-22 | Tutor workflow: deploy the Expo Web static site on Render | Created and tested the Render Static Site, reviewed how the deployed frontend and API are separate services connected by `EXPO_PUBLIC_API_URL`, and clarified why the locally generated `.env.local` IP address is not committed or used by Render. Discussed presenting the deployed site in a centered mobile-width layout for recruiters. | Render builds the Expo frontend into `apps/mobile/dist` and serves it as static files, while the browser calls the separately deployed Express API. Render keeps its environment variables in the service configuration, and the gitignored local `.env.local` can continue using a development IP. A dedicated Render-only public flag is clearer than using development mode to control the mobile-width web presentation. | Tomorrow, add a Render-only layout flag and use it with a web-platform check in the root layout so only the deployed static site receives the centered mobile-width presentation. |
| 2026-06-23 | Tutor workflow: Render demo layout and custom exercise start | Confirmed the Render-only mobile-width web layout works with `EXPO_PUBLIC_DEMO_LAYOUT=true`, then started the custom exercise feature. Added an `Add +` button concept on the exercise selection screen, reviewed the new `addNewExercise(newExercise: ExerciseData)` DB function, and discussed why Knex `insert().returning(...)` returns an array even when inserting one row. | The inserted exercise helper should return one `Exercise`, so `const [newExerciseAdded] = await ...returning(...)` uses array destructuring to take the first returned row. If the function promised `Exercise[]`, it would keep the full returned array instead. The next layer is the route: `POST /exercises` should validate `name` and `muscleGroup`, call `db.addNewExercise(req.body)`, and return `201` with the created exercise. | Resume in `apps/api/server/routes/exercises.ts`: finish the incomplete `router.post('/')` block, add validation for missing `name` or `muscleGroup`, return `400` for invalid input, call `db.addNewExercise(newExercise)`, and return `201` JSON. Then wire the mobile `Add +` button to a form or new screen. |
| 2026-06-24 | Tutor workflow: custom exercise mobile flow | Finished reviewing the backend `POST /exercises` route validation, clarified why request bodies use camelCase model keys like `muscleGroup` while the DB helper maps to `muscle_group`, decided to keep the add-exercise screen inside the workout flow, created `apps/mobile/app/workout/add-exercise.tsx`, wired the `Add +` button concept in `apps/mobile/app/workout/exercises.tsx`, and started `apps/mobile/components/workout/NewExerciseForm.tsx` with a `NewExerciseFormProps` type. | The API contract should use the app/model shape, while DB helpers translate to database columns. A custom exercise is a reusable catalogue item, even if it is created from inside an active workout. The add-exercise screen needs `useLocalSearchParams()` for `workoutId` and `muscleGroup`; `workoutId` is used to continue to `log-set`, and `muscleGroup` is used in the `POST /exercises` body. `onPress` must receive a function, such as `() => router.push(...)`. Expo Router may show a stale pathname type error until the new route returns JSX and route types regenerate. | Resume with `NewExerciseForm.tsx`: decide whether the form owns `exerciseName` state or receives it via props from `add-exercise.tsx`, then render the form, submit `POST /exercises` with `{ name, muscleGroup }`, use the returned `exercise.id`, and navigate to `/workout/log-set` with `workoutId`, `muscleGroup`, and `exerciseId`. |
| 2026-06-25 | Tutor workflow: NewExerciseForm and muscle group dropdown planning | Refreshed the previous session, reviewed `NewExerciseForm.tsx`, `add-exercise.tsx`, `SetForm.tsx`, the exercise route, and the exercise DB helper. Decided that `NewExerciseForm` should stay controlled and reusable, while `add-exercise.tsx` owns state, API submission, and navigation. Discussed adding a muscle group dropdown with the current route muscle group preselected, allowing the user to choose another group, and always navigating to `log-set` for the newly created exercise after success. Started reviewing a new `getAllMuscleGroups()` DB helper. | A route screen should own route params, fetches, loading/error state, and navigation. A form component should mostly render values and call callbacks. For muscle groups, a backend endpoint is cleaner than having the mobile screen fetch all exercises just to derive dropdown options. Knex returns selected rows as objects even when selecting one column, so a query like `select muscle_group as muscleGroup` returns `{ muscleGroup: string }[]`; mapping with `rows.map((row) => row.muscleGroup)` converts that to `string[]`. The query should also use `distinct` to avoid duplicates and ordering to keep the dropdown stable. | Tomorrow, finish `getAllMuscleGroups()` in `apps/api/server/db/exercises.ts`, add `GET /exercises/muscle-groups` in the exercises route, then continue the mobile steps: fetch muscle group options in `add-exercise.tsx`, control `exerciseName` and selected `muscleGroup`, render `NewExerciseForm`, submit `POST /exercises`, and navigate to `/workout/log-set` with `workoutId`, selected `muscleGroup`, and the returned `exerciseId`. |
| 2026-06-26 | Tutor workflow: implement custom exercise flow and plan gym context | Installed and opened the iOS Simulator workflow, ran the API and Expo on iOS, finished `GET /exercises/muscle-groups`, completed the controlled add-exercise screen and dropdown form, added a success toast, changed success navigation to `router.replace`, fixed stale exercise-list data with `useFocusEffect`, and discussed the future gym/equipment model. | I learned that `router.push` keeps the previous screen mounted, so going Back can reveal stale form state; `router.replace` is better when the completed form should not remain in the navigation stack. I learned that an inline dropdown can either expand layout or overlay content depending on whether its menu participates in normal layout; absolute positioning with `zIndex`/`elevation` makes it float over the button instead of pushing it down. I also learned that `useEffect(..., [])` only loads data once, while `useFocusEffect` is the right pattern when a screen should refresh after returning to it. For modelling, I clarified that generic exercises such as `Lat Pulldown` should stay separate from gym-specific equipment such as `Pulldown 1` at `City Fitness`. | After compaction, resume from the gym-context design. Start with a small workout-level gym slice: decide the first gym source/default, then show a small gym tag under the title on `log-set.tsx` and near the top of `[workoutId].tsx`. Keep existing pages as the visual source of truth. |
| 2026-06-26 | Save workflow: dark theme and workspace handoff | Committed and pushed `Add app dark mode settings` on `main`, added persistent theme state with `AsyncStorage`, added a `Settings` tab beside `Explore`, and themed the main workout screens/components with semantic colors. | The app-wide theme now comes from a custom context rather than the raw system color scheme hook, which makes the setting persistent and keeps the navigation theme in sync with the UI. The remaining uncommitted package-manifest changes are unrelated workspace noise: `package.json` and `package-lock.json` picked up Playwright, while the feature work itself is already on GitHub. | If resuming, first decide whether to keep or discard the unrelated manifest changes, then continue the workout/gym context work by adding a small workout-level gym tag under `log-set` and on the workout detail screen. |
| 2026-08-30 | Repo cleanup: retire promptkit, consolidate CLAUDE.md, add a `save` skill | Deleted `promptkit/` (tutor/reflect workflows, protocols, activities, learning-plan, session-memory), keeping only the journal by moving it to `PROGRESS_JOURNAL.md` at the repo root. Merged `AGENTS.md` and `GEMINI.md` into a single new `CLAUDE.md` with an explicit working-style rule: for feature work, give file paths, a code snippet, and an explanation instead of editing files directly, since this project is for learning. Deleted `AGENTS.md`/`GEMINI.md` and committed the cleanup. Then created `.claude/skills/save/SKILL.md`, a project skill that appends a journal row, commits, and pushes to GitHub. | I learned that Claude Code project skills live at `.claude/skills/<name>/SKILL.md` with `name`/`description` frontmatter, and that the skill list is only loaded at session start, so a newly added skill needs a session restart before it shows up. I also learned `promptkit/` was already gitignored and carried its own nested `.git`, so deleting it didn't touch the main repo's tracked history at all. | Restart the Claude Code session so `/save` is registered, then use `/save` for real at the end of the next work session to confirm the drafted-entry-plus-commit-plus-push flow works end to end. |

## Reflection Prompts
- What surprised me today?
- How did the Tutor workflow help?
- What should I bring to the next stand-up or mentoring session?
